# Production env (safe defaults; no secrets)

APP_ENVIRONMENT=production
APP_LOG_LEVEL=INFO
# APP_LOG_FORMAT can be: json | text
APP_LOG_FORMAT=json
APP_SERVICE_NAME=conversational-connect
APP_API_V1_PREFIX=/api/v1
